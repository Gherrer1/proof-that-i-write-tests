const chai = require('chai');
const should = chai.should();
const sinon = require('sinon');
const request = require('supertest');
const app = require('../../app');
const seed = require('../../seed');

describe('Authentication Routes', function() {
  // beforeEach(function(done) {
  //   seed.connect(function() {
  //     seed.seed()
  //     .then(done, done);
  //   });
  // });

  // beforeEach(function() {
  //   // throw new Error('bitch ass');
  //   // done(new Error('youre a cunt'));
  // });

  it('should yata yata yata', function() {
    throw new Error('cunt ass');
  });

  it('should throw yet another erro', function() {
    // throw new Error('estephany');
  });

  it('should throw a 3rd', () => {
    // throw new Error('baaaah');
  });

  // afterEach(function(done) {
  //   seed.disconnect(done);
  // });
});
